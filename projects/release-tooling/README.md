# Release Tooling

Assemble release packages from frozen or generated artifact roots.

Each logically distinct scenario should be passed as a separate `--input-root` to preserve comparison granularity.
